name: app_movil_wati
description: "WATI - Tu energía, bajo control."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Icons
  cupertino_icons: ^1.0.8
  firebase_core: ^4.9.0
  firebase_auth: ^6.5.1

  # UI & Design
  google_fonts: ^6.1.0
  flutter_animate: ^4.5.0
  shimmer: ^3.0.0

  # State Management
  flutter_bloc: ^8.1.5
  bloc: ^8.1.4

  # Navigation
  go_router: ^14.2.0

  # Network
  dio: ^5.4.3+1

  # Charts
  fl_chart: ^0.68.0

  # Storage
  flutter_secure_storage: ^9.2.2

  # Utils
  intl: ^0.19.0
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true